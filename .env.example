# Docker Compose Environment Variables
# Copy this to .env and customize as needed

# Ports (host:container mapping done in docker-compose.yml)
FRONTEND_PORT=5173
BACKEND_PORT=8080
MONGODB_PORT=27017

# MongoDB
MONGO_DATABASE=vladtechdb

# Spring Profile
SPRING_PROFILES_ACTIVE=docker
